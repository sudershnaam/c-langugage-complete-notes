What is C
What is C?
C is a general-purpose programming language created by Dennis Ritchie at Bell Labs in 1972.
It is popular because it is fast, widely supported, and helps you understand how programs work "under the hood".
The main reason for its popularity is because it is a fundamental language in the field of computer science.
C is closely connected to UNIX, because much of UNIX was written in C.
________________


What is C used for?
C is often used to create programs that need to run fast and work closely with the computer.
* Operating systems, such as parts of Windows, Linux, and macOS
* Programs inside devices like cars, TVs, and home electronics
* Software that needs to be fast, including databases and system tools
* Game engines and programs that handle graphics
* Core libraries that other programming languages rely on
Why Learn C?
* It is one of the most widely used programming languages
* If you know C, you will have no problem learning other popular programming languages such as Java, Python, C++, C#, etc, as the syntax is similar
* It helps you understand memory, performance, and how computers handle data
* C is very versatile; it can be used in both applications and technologies
________________


Difference between C and C++
* C++ was developed as an extension of C, and the syntax is often similar
* C is mainly a procedural language (you build programs with functions)
* C++ supports classes and objects (often used for object-oriented programming)


Tab 2


________________


💻 1. Windows Setup (Most common)
Step 1: Install a Compiler (MinGW)
1. Go to the official MinGW site:
👉 https://www.mingw-w64.org/
2. Download the installer (look for mingw-w64-install.exe).
3. Run the installer:
   * Architecture: x86_64
   * Threads: posix
   * Exception: seh
Install it in a simple path like:
C:\mingw-w64
   4. ________________


Step 2: Add to PATH
   1. Open Start Menu → Search “Environment Variables”
   2. Click Edit the system environment variables
   3. Click Environment Variables
   4. Under System Variables, find Path → Edit
Add:
C:\mingw-w64\bin
   5.    6. Click OK and restart your PC
________________


Step 3: Verify Installation
   1. Open Command Prompt
Type:
gcc --version
   2.    3. If it shows version info → ✅ success
________________


Step 4: Install a Code Editor
You can use:
   * Visual Studio Code (recommended)
   * Notepad++ (simple option)
Install VS Code and then install the C/C++ extension inside it.
________________


Step 5: Run Your First Program
   1. Open VS Code
   2. Create file: hello.c
Add code:
#include <stdio.h>


int main() {
    printf("Hello, World!");
    return 0;
}
   3. Open terminal and run:
gcc hello.c -o hello
hello
   4. ________________


🍎 2. macOS Setup
Step 1: Install Xcode Command Line Tools
Open Terminal and run:
xcode-select --install


________________


Step 2: Verify
gcc --version


________________


Step 3: Run Program
gcc hello.c -o hello
./hello


________________


🐧 3. Linux Setup (Ubuntu/Debian)
Step 1: Install GCC
Open terminal:
sudo apt update
sudo apt install build-essential


________________


Step 2: Verify
gcc --version


________________


Step 3: Run Program
gcc hello.c -o hello
./hello


________________


⚠️ Common Problems & Fixes
   * 'gcc not recognized' (Windows)
→ PATH not set correctly
Permission denied (Linux/macOS)
→ Use:
chmod +x hello
      *       * Wrong folder
→ Make sure you're in the file directory before compiling
________________


✅ Quick Summary
         * Install compiler (GCC / MinGW)
         * Set PATH (Windows only)
         * Install editor (VS Code recommended)
Compile using:
gcc filename.c -o output
________________